#include "list.h"
#include <stdlib.h>
#include <string.h>

Node* create_node(Book* book) {
    Node* node = (Node *) malloc(sizeof (Node));
    strncpy(node -> book.title, book -> title, 50);
    strncpy
    strncpy
}
